import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    margin: 10,
  },
  boxIcon: {
    backgroundColor: "black",
    padding: 10,
    borderRadius: 25,
    margin: 10,
  },
});
